// Step 1: npm init
// Step 2: npm install prompt-sync

const prompt = require("prompt-sync")();


// Rudolf

const details = {
    my: 'name',
    is: 'Rudolf',
    the: 'raindeer'
  }


  for (var key in details) {
    console.log(key) + console.log(details[key]); 
        }; 
    
